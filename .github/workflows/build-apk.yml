name: Build APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
        
    - name: Setup Android SDK
      uses: android-actions/setup-android@v3
      with:
        packages: 'platform-tools platforms;android-34 build-tools;34.0.0'
        accept-android-sdk-licenses: true
        
    - name: Setup Gradle Wrapper
      run: |
        # Verifica se gradlew existe
        if [ ! -f "./gradlew" ]; then
          echo "❌ gradlew não encontrado no repositório!"
          exit 1
        fi
        
        # Verifica se gradle-wrapper.jar existe, se não, baixa ou gera
        if [ ! -f "gradle/wrapper/gradle-wrapper.jar" ]; then
          echo "📥 gradle-wrapper.jar não encontrado. Tentando baixar..."
          mkdir -p gradle/wrapper
          
          # Tenta baixar da URL do GitHub (versão específica 8.2)
          curl -f -L -o gradle/wrapper/gradle-wrapper.jar "https://raw.githubusercontent.com/gradle/gradle/v8.2.0/gradle/wrapper/gradle-wrapper.jar" || {
            echo "⚠️ Falha na primeira URL, tentando URL alternativa..."
            # Tenta URL alternativa do Gradle
            curl -f -L -o gradle/wrapper/gradle-wrapper.jar "https://services.gradle.org/distributions/gradle-wrapper.jar" || {
              echo "⚠️ Falha nas URLs, tentando gerar wrapper com Gradle instalado..."
              # Última tentativa: usar Gradle instalado para gerar o wrapper
              if command -v gradle &> /dev/null; then
                GRADLE_VERSION=$(grep "distributionUrl" gradle/wrapper/gradle-wrapper.properties | sed -n 's/.*gradle-\([0-9.]*\)-.*/\1/p' || echo "8.2")
                gradle wrapper --gradle-version "$GRADLE_VERSION" || {
                  echo "❌ Erro: Não foi possível gerar gradle-wrapper.jar"
                  exit 1
                }
              else
                echo "❌ Erro: Não foi possível baixar ou gerar gradle-wrapper.jar"
                exit 1
              fi
            }
          }
          
          # Verifica se o arquivo foi criado e não está vazio
          if [ ! -f "gradle/wrapper/gradle-wrapper.jar" ] || [ ! -s "gradle/wrapper/gradle-wrapper.jar" ]; then
            echo "❌ Erro: gradle-wrapper.jar está vazio ou não existe"
            exit 1
          fi
          
          echo "✅ gradle-wrapper.jar configurado com sucesso"
          ls -lh gradle/wrapper/gradle-wrapper.jar
        else
          echo "✅ gradle-wrapper.jar já existe"
          ls -lh gradle/wrapper/gradle-wrapper.jar
        fi
        
        chmod +x ./gradlew
        echo "✅ Gradle wrapper pronto para uso"
        
    - name: Cache Gradle dependencies
      uses: actions/cache@v4
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-
      
    - name: Build Release APK
      run: ./gradlew assembleRelease --stacktrace --no-daemon --warning-mode all
      env:
        ANDROID_HOME: ${{ env.ANDROID_HOME }}
        ANDROID_SDK_ROOT: ${{ env.ANDROID_SDK_ROOT }}
      
    - name: Check APK exists
      run: |
        if [ ! -f "app/build/outputs/apk/release/app-release.apk" ]; then
          echo "APK não encontrado! Verificando estrutura de diretórios..."
          find app/build -name "*.apk" || echo "Nenhum APK encontrado"
          ls -la app/build/outputs/apk/ || echo "Diretório não existe"
          exit 1
        fi
        ls -lh app/build/outputs/apk/release/app-release.apk
        echo "✅ APK gerado com sucesso!"
      
    - name: Upload APK artifact
      uses: actions/upload-artifact@v4
      with:
        name: app-release-apk
        path: app/build/outputs/apk/release/app-release.apk
        retention-days: 30
        if-no-files-found: error
        
    - name: Generate build summary
      run: |
        echo "## 📦 Build APK - Sucesso!" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "- **Commit**: \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
        echo "- **Branch**: \`${{ github.ref_name }}\`" >> $GITHUB_STEP_SUMMARY
        echo "- **Build Date**: $(date -u +'%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### 📥 Download APK" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "O APK está disponível na seção de **Artifacts** abaixo." >> $GITHUB_STEP_SUMMARY
