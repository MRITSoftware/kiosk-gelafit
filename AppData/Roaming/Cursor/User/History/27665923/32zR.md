# ✅ PHP Instalado! Próximos Passos

## 🎉 Status
- ✅ **XAMPP instalado** (PHP 8.2.12)
- ✅ **PHP funcionando** em `C:\xampp\php\php.exe`

## 🚀 Passos para Testar

### 1. Iniciar XAMPP
1. Abra o **XAMPP Control Panel**
2. Clique em **Start** para:
   - ✅ **Apache**
   - ✅ **MySQL**

### 2. Criar Banco de Dados
1. Acesse: **http://localhost/phpmyadmin**
2. Clique em **Novo** (New)
3. Nome: `esign_test`
4. Clique em **Criar**

### 3. Copiar Projeto para XAMPP

**Opção A: Manualmente**
- Copie a pasta `php-version` para `C:\xampp\htdocs\esign`

**Opção B: Via PowerShell** (execute como Administrador)
```powershell
# Permitir execução de scripts (uma vez)
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# Copiar projeto
Copy-Item "D:\ass digital\php-version" -Destination "C:\xampp\htdocs\esign" -Recurse
```

### 4. Configurar Banco de Dados

Edite `C:\xampp\htdocs\esign\config\database.php`:
```php
return [
    'host' => 'localhost',
    'database' => 'esign_test',
    'username' => 'root',
    'password' => '', // XAMPP geralmente não tem senha
    'charset' => 'utf8mb4',
    'options' => [
        PDO::ATTR_ERRMODE => PDO::ERRMODE_EXCEPTION,
        PDO::ATTR_DEFAULT_FETCH_MODE => PDO::FETCH_ASSOC,
        PDO::ATTR_EMULATE_PREPARES => false,
    ]
];
```

### 5. Configurar Ambiente

Edite `C:\xampp\htdocs\esign\config\config.php`:
```php
define('ENVIRONMENT', 'development');
define('BASE_URL', 'http://localhost/esign');
// ... resto já está configurado
```

### 6. Instalar Dependências (Composer)

**Se você tem Composer:**
```powershell
cd C:\xampp\htdocs\esign
composer install --no-dev
```

**Se não tem Composer:**
1. Baixe: https://getcomposer.org/download/
2. Instale
3. Depois execute: `composer install`

### 7. Testar!

Acesse no navegador:
- **http://localhost/esign/test.php**

Deve ver todas as verificações passando ✅

---

## 🧪 Testar API

### Teste de Cadastro:
```powershell
curl -X POST http://localhost/esign/api/auth/cadastro.php `
  -H "Content-Type: application/json" `
  -d '{\"cpf\":\"12345678901\",\"nome\":\"Teste\",\"email\":\"teste@teste.com\"}'
```

### Teste de Login:
```powershell
curl -X POST http://localhost/esign/api/auth/login.php `
  -H "Content-Type: application/json" `
  -d '{\"cpf\":\"12345678901\"}'
```

---

## 📝 Resumo dos Comandos

```powershell
# 1. Copiar projeto (se ainda não copiou)
Copy-Item "D:\ass digital\php-version" -Destination "C:\xampp\htdocs\esign" -Recurse

# 2. Ir para pasta do projeto
cd C:\xampp\htdocs\esign

# 3. Instalar dependências
composer install --no-dev

# 4. Testar
# Acesse: http://localhost/esign/test.php
```

---

## ✅ Checklist

- [ ] XAMPP instalado e Apache/MySQL rodando
- [ ] Banco `esign_test` criado no phpMyAdmin
- [ ] Projeto copiado para `C:\xampp\htdocs\esign`
- [ ] `config/database.php` configurado
- [ ] `config/config.php` configurado para desenvolvimento
- [ ] Composer instalado e dependências instaladas
- [ ] Teste em http://localhost/esign/test.php funciona

---

## 🐛 Problemas?

### "Porta 80 em uso"
- Feche outros servidores web
- Ou mude a porta do Apache no XAMPP

### "Composer não encontrado"
- Baixe e instale: https://getcomposer.org/download/

### "Erro de conexão com banco"
- Verifique se MySQL está rodando no XAMPP
- Verifique se o banco `esign_test` foi criado
- Verifique credenciais em `config/database.php`

---

**Pronto! Agora você pode testar localmente!** 🎉

