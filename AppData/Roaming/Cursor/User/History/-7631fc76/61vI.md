# 🚀 Guia de Deploy - Extrator de Tabelas PDF

## Opções de Hospedagem

### 1. 🌟 HEROKU (Recomendado - Gratuito)

**Passos:**
1. **Criar conta no Heroku:** https://heroku.com
2. **Instalar Heroku CLI:** https://devcenter.heroku.com/articles/heroku-cli
3. **Fazer login:**
   ```bash
   heroku login
   ```
4. **Criar app:**
   ```bash
   heroku create seu-app-extrator-pdf
   ```
5. **Fazer deploy:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git push heroku main
   ```

**✅ Vantagens:**
- Gratuito (com limitações)
- Deploy automático
- Suporte nativo ao Python
- Fácil de configurar

### 2. 🖥️ HOSTGATOR VPS

**Se você tem VPS no HostGator:**

1. **Conectar via SSH:**
   ```bash
   ssh usuario@seu-servidor.com
   ```

2. **Instalar dependências:**
   ```bash
   sudo apt update
   sudo apt install python3 python3-pip nginx
   pip3 install -r requirements.txt
   ```

3. **Configurar Nginx:**
   ```bash
   sudo nano /etc/nginx/sites-available/extrator-pdf
   ```
   
   Conteúdo:
   ```nginx
   server {
       listen 80;
       server_name seu-dominio.com;
       
       location / {
           proxy_pass http://127.0.0.1:5000;
           proxy_set_header Host $host;
           proxy_set_header X-Real-IP $remote_addr;
       }
   }
   ```

4. **Iniciar aplicação:**
   ```bash
   gunicorn -w 4 -b 127.0.0.1:5000 app:app
   ```

### 3. 🌐 OUTRAS OPÇÕES

**Railway (Gratuito):**
- https://railway.app
- Conectar GitHub e fazer deploy automático

**Render (Gratuito):**
- https://render.com
- Deploy direto do GitHub

**PythonAnywhere (Gratuito):**
- https://pythonanywhere.com
- Upload de arquivos via interface web

## 📁 Arquivos Necessários

Certifique-se de ter estes arquivos na raiz do projeto:
- `app.py` (aplicação principal)
- `requirements.txt` (dependências)
- `Procfile` (configuração do Heroku)
- `runtime.txt` (versão do Python)
- `templates/` (pasta com HTML)
- `uploads/` (pasta para uploads - será criada automaticamente)

## 🔧 Configurações Importantes

### Variáveis de Ambiente (Opcional)
```bash
# Para produção
export FLASK_ENV=production
export SECRET_KEY=sua-chave-secreta-aqui
```

### Limitações do Heroku Gratuito
- App "dorme" após 30 min de inatividade
- Máximo 550 horas/mês
- Sem persistência de arquivos (uploads são perdidos)

### Para Produção Real
- Use banco de dados (PostgreSQL)
- Configure armazenamento permanente (AWS S3)
- Use domínio personalizado
- Configure HTTPS

## 🚀 Deploy Rápido no Heroku

```bash
# 1. Instalar Heroku CLI
# 2. Fazer login
heroku login

# 3. Criar app
heroku create meu-extrator-pdf

# 4. Deploy
git init
git add .
git commit -m "Deploy inicial"
git push heroku main

# 5. Abrir app
heroku open
```

## 📞 Suporte

Se precisar de ajuda com o deploy, me avise!
