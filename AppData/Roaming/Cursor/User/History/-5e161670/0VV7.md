# 🤖 Configuração Automática com Automate (Android)

## O que é o Automate?
App gratuito para Android que permite automatizar tarefas, como iniciar scripts quando o dispositivo liga ou quando a internet volta.

## 📥 Instalação do Automate

1. Baixe o **Automate** na Play Store (gratuito)
2. Abra o app e permita as permissões necessárias

## ⚙️ Configuração do Fluxo

### Opção 1: Iniciar ao Ligar o Tablet

1. Abra o Automate
2. Toque em **"+"** (criar novo fluxo)
3. Arraste os blocos na seguinte ordem:

```
┌─────────────────────┐
│ Device boot        │ → Quando o tablet liga
└─────────────────────┘
         ↓
┌─────────────────────┐
│ Shell command      │ → Executa: bash ~/servidorzinho/iniciar_auto.sh
└─────────────────────┘
```

**Configuração do bloco "Shell command":**
- Command: `bash`
- Arguments: `~/servidorzinho/iniciar_auto.sh`
- Working directory: `~/servidorzinho`

### Opção 2: Reiniciar quando Internet Volta

1. Crie um novo fluxo
2. Configure:

```
┌─────────────────────┐
│ Connectivity       │ → Monitora conexão Wi-Fi
│ changed            │
└─────────────────────┘
         ↓
┌─────────────────────┐
│ If                 │ → Se conectado
│ (Wi-Fi connected)  │
└─────────────────────┘
         ↓
┌─────────────────────┐
│ Shell command      │ → bash ~/servidorzinho/iniciar_auto.sh
└─────────────────────┘
```

## 🔄 Alternativa: Usar Tasker (Pago)

Se preferir usar Tasker (mais poderoso, mas pago):

1. Instale o Tasker
2. Crie um perfil:
   - **Trigger**: Device Boot
   - **Action**: Run Shell
   - **Command**: `bash ~/servidorzinho/iniciar_auto.sh`

## 💡 Dica: Iniciar Manualmente no Termux

Se preferir não usar apps externos, você pode adicionar ao `~/.bashrc`:

```bash
# Inicia servidor automaticamente ao abrir Termux
if [ ! -f ~/servidorzinho/servidor.pid ] || ! ps -p $(cat ~/servidorzinho/servidor.pid) > /dev/null 2>&1; then
    cd ~/servidorzinho && bash iniciar_auto.sh
fi
```

## 📱 Verificar se está rodando

No Termux:
```bash
ps aux | grep servidor_auto.py
```

Ou verificar o log:
```bash
tail -f ~/servidorzinho/servidor.log
```

