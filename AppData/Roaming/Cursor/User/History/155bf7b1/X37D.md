# 🔍 Como Verificar se os Arquivos Foram Copiados

## No Termux

### 1. Verificar se os arquivos estão na pasta home (~/)

```bash
# Listar todos os arquivos Python, TXT e SH
ls -lh ~/*.py ~/*.txt ~/*.sh

# Ou verificar arquivo por arquivo
ls -lh ~/tuya_server_enhanced.py
ls -lh ~/requirements.txt
ls -lh ~/start_server.sh
ls -lh ~/stop_server.sh
```

### 2. Se os arquivos NÃO estiverem na home

Os arquivos podem estar em Downloads. Para copiar:

```bash
# Verificar Downloads
ls -lh ~/storage/downloads/

# Copiar para home
cp ~/storage/downloads/tuya_server_enhanced.py ~/
cp ~/storage/downloads/requirements.txt ~/
cp ~/storage/downloads/start_server.sh ~/
cp ~/storage/downloads/stop_server.sh ~/

# Dar permissão de execução aos scripts
chmod +x ~/*.sh

# Verificar novamente
ls -lh ~/*.py ~/*.txt ~/*.sh
```

### 3. Verificar conteúdo dos arquivos

```bash
# Ver primeiras linhas do servidor
head -n 10 ~/tuya_server_enhanced.py

# Ver requirements
cat ~/requirements.txt
```

## No App Android

1. **Abra o app "Tuya Installer"**
2. **Clique em "2️⃣ Copiar Arquivos"**
3. **Veja o log** - ele mostra onde os arquivos foram copiados
4. **Se aparecer "Downloads"**, use os comandos acima para copiar

## Problemas Comuns

### Arquivos não aparecem

```bash
# Verificar se Termux tem permissões
ls -la ~/

# Verificar Downloads
ls -la ~/storage/downloads/

# Procurar em todos os lugares
find ~/ -name "tuya_server_enhanced.py" 2>/dev/null
```

### Erro de permissão

```bash
# Dar permissão de leitura/execução
chmod +x ~/*.sh
chmod 644 ~/*.py ~/*.txt
```

### Arquivos corrompidos

Se os arquivos existem mas estão vazios ou corrompidos:

1. No app, clique novamente em "2️⃣ Copiar Arquivos"
2. Ou copie manualmente de Downloads

## Comando Completo de Verificação

```bash
echo "=== Verificando arquivos ===" && \
echo "" && \
echo "Na home (~/):" && \
ls -lh ~/tuya_server_enhanced.py ~/requirements.txt ~/start_server.sh ~/stop_server.sh 2>/dev/null || echo "Arquivos não encontrados em ~/" && \
echo "" && \
echo "Em Downloads:" && \
ls -lh ~/storage/downloads/tuya_server_enhanced.py ~/storage/downloads/requirements.txt 2>/dev/null || echo "Arquivos não encontrados em Downloads"
```

---

**Dica:** Se os arquivos não estiverem em lugar nenhum, use o botão "2️⃣ Copiar Arquivos" novamente no app!

